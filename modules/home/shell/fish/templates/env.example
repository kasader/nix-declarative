# Copy to .env and fill in real values.
#
# .env is gitignored and loaded automatically by direnv (see .envrc).
# Keep THIS file committed; keep .env out of git.

# Generic API token (See: https://example.com/settings/tokens)
# EXAMPLE_API_TOKEN=
